/** Single source of truth for referral tuning — imported by both this module and bookings.service.ts. */
export const REFEREE_FIRST_BOOKING_DISCOUNT = 100; // flat ₹ off referee's first booking — tune later
export const REFERRAL_CODE_EXPIRY_DAYS = 30; // window from the referee's account creation
export const REFERRER_REWARD_AMOUNT = 0; // placeholder — actual redemption amount TBD
export const REFERRER_REWARD_REASON = "referral_qualified";
