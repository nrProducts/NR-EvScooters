# Generated by scripts/build-legal.mjs from docs/legal/*.md.
#
# Deliberately NOT committed while the source documents still carry
# [PLACEHOLDERS] and await legal review — a privacy policy that deploys
# saying "[GRIEVANCE EMAIL]" is worse than no page at all, and Google Play
# reads this page during review.
#
# Once the brackets are filled and a lawyer has signed the markdown off:
#   1. delete these two lines
#   2. node scripts/build-legal.mjs      (it refuses while placeholders remain)
#   3. commit public/privacy.html and public/terms.html
public/privacy.html
public/terms.html
