/**
 * The rider-facing scooter shot (transparent PNG) used on Home — the hero card
 * and the KYC / rental-completed states. A single bundled asset so the vehicle
 * always reads at brand quality regardless of what the catalog API returns.
 *
 * Swap apps/mobile/assets/images/blue-motovolt-scooter.png to change it.
 */
export const SCOOTER_HERO = require('../../assets/images/blue-motovolt-scooter.png') as number;
